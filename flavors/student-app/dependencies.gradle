ext {

    libs = [
            constraint : uiDeps.constraint,
            navFragment: uiDeps.nav_fragment,
            navKTx     : uiDeps.nav_ktx,

            dagger            : deps.dagger,
            dagger_android    : deps.dagger_android,
            dagger_support    : deps.dagger_support,

            room_runtime      : deps.roomRuntime,
            room_ktx          : deps.roomKtx,
            room_rxjava2      : deps.roomRxJava2,

            rxJava            : deps.rxJava,
            rxKotlin          : deps.rxKotlin,
            rxAndroid         : deps.rxJavaAndroid,

            retrofit          : deps.retrofit,
            retrofit_adapter  : deps.rxJava_adapter,
            retrofit_converter: deps.gson_converter,

            okhttp_logger     : deps.okhttp_logger,

            gson              : deps.gson,
    ]
}



